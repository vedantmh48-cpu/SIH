# --- Core web framework ---
fastapi>=0.115.0
uvicorn[standard]>=0.30.0
python-multipart>=0.0.9
python-dotenv>=1.0.1
httpx>=0.27.0

# --- Database ---
pymongo>=4.8.0

# --- Auth (v1 slice) ---
PyJWT>=2.9.0
# IANA timezone database for Python's zoneinfo (required for ZoneInfo("Asia/Kolkata")
# used by the mailer on Windows / minimal images).
tzdata>=2024.1
# Optional production infrastructure (auto-detected at runtime).
# PostgreSQL/PostGIS + Redis power the v1 auth store & active-session cache;
# without them SatQuery AI transparently falls back to the legacy store so
# all auth flows still work locally.
# psycopg[binary]>=3.2       # PostgreSQL/PostGIS auth tables
# redis>=5.0                 # active session tracking

# --- Auth ---
# (kept above with the v1 slice)

# --- Report exports ---
reportlab>=4.2,<6          # server-side PDF generation

# --- Optional: Google Docs report publishing (OAuth) ---
google-auth>=2.35
google-auth-oauthlib>=1.2
google-api-python-client>=2.150

# --- Testing ---
pytest>=8.3.0

# =====================================================================
# OPTIONAL integrations (auto-detected at runtime).
# If not installed, SatQuery AI runs in "local demo mode" with clearly
# labelled simulated datasets. Install them when you want real processing
# or heavier AI/geo pipelines (many require Python <=3.12 wheels).
# =====================================================================
# numpy>=1.26            # numeric processing acceleration
# scipy>=1.13
# rasterio>=1.3.9        # raster I/O (needs GDAL wheels)
# geopandas>=0.14        # vector processing
# shapely>=2.0
# faiss-cpu>=1.8         # vector similarity index
# langchain>=0.2          # LLM orchestration
# langgraph>=0.2
# reportlab>=4.2          # server-side PDF generation